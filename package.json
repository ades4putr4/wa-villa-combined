{
  "name": "zn-villa-site-bot",
  "version": "1.0.0",
  "description": "Website ZN Villa Gedong Songo + bot auto-reply WhatsApp, dalam satu deploy Cloudflare Workers.",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
